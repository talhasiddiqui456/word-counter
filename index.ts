// // src/index.ts

// import { WordCounter } from './wordCounter';
// import { FileReader } from './fileReader'; // Optional

// const wordCounter = new WordCounter();
// const fileReader = new FileReader(); // Optional

// // Sample text for counting
// const sampleText = "This is a simple example of a word counter in TypeScript.";

// // Count words in a string
// console.log(`Word count: ${wordCounter.countWords(sampleText)}`);

// // (Optional) Count words in a file
// const filePath = 'path/to/your/textfile.txt'; // Replace with your file path
// const fileContent = fileReader.readFile(filePath);
// if (fileContent) {
//     console.log(`Word count in file: ${wordCounter.countWords(fileContent)}`);
// }
